/**
 * This modal lets the user import the save file from a file or a textbox.
 */
export const ImportSaveModal = () => {
    return (
        <div
            className="modal save-modal"
            data-testid="import-save-modal"
        ></div>
    );
};
