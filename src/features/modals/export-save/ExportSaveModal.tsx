/**
 * This modal lets the user export the save file into a data format and save to a file.
 */
export const ExportSaveModal = () => {
    return (
        <div id="export-save-modal" data-testid="export-save-modal">
            Export Save Modal
        </div>
    );
};
