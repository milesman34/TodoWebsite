import { describe, expect, test } from "vitest";

describe("Root", () => {
    describe("Clicking the All Tasks button must display all the tasks", () => {
        test("Clicking the All Tasks button displays all the tasks", () => {
            // const tasks
            expect(3).toBe(3); 
        });
    });
});
